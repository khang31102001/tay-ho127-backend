# Database Rules

## Core Rules
- Model data from business invariants, not only UI screens.
- Use clear primary keys, constraints, indexes, and relationship ownership.
- Keep migrations deterministic and reversible where practical.
- Avoid premature denormalization.
- Treat audit fields and soft delete as explicit design decisions.
