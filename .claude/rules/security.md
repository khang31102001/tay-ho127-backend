# Security Rules

## Core Rules
- Never commit secrets.
- Validate and authorize at server boundaries.
- Use least privilege for roles, permissions, and data access.
- Avoid exposing internal error details.
- Treat authentication, authorization, logging, and input validation as separate concerns.
