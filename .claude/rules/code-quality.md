# Code Quality Rules

## Core Rules
- Prefer readable, explicit code over clever abstractions.
- Keep responsibilities clear.
- Avoid `any`, `@ts-ignore`, and hidden type escapes without justification.
- Do not split files only because they are long.
- Remove dead code after safe refactors.
- Make the smallest correct change and verify affected consumers.
